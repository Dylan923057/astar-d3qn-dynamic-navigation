inherits: dynamic_spatial_generalization_parcel_balanced_v4.yaml

experiment:
  name: dynamic_spatial_generalization_parcel_conflict_adaptive_v1
  output_root: outputs/dynamic_spatial_generalization_parcel_conflict_adaptive_v1
  run_name_prefix: dynamic_spatial_generalization_parcel_ca

training:
  conflict_demo_fraction_min: 0.0
  conflict_ema_alpha: 0.2
  conflict_sensitivity: 1.0
  conflict_predict_next: true
